KCron

KDE Task Scheduler

GUI crontab editor

Requires:

  Unix POSIX libraries for localized dates and times (glibc)
  Cron (vixie-cron)
  Crontab (crontabs)
  Qt 1.4.4 or higher
  KDE 1.1.1 or higher

  Note: For Qt 1.x and KDE 1.x, edit the Makefile (CPPFLAGS) to compile with
  "-DKDE1".

